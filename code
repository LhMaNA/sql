cols_numeriques = df.select_dtypes(include=[np.number]).columns.tolist()
cols_numeriques = [c for c in cols_numeriques if c not in
                   ('code_postale_emplacement', 'longitude', 'latitude')]
